import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demswal.app',
  appName: 'demo-swal',
  webDir: 'dist/demoSwal',
  bundledWebRuntime: false
};

export default config;
